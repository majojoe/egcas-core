#ifndef EGCEXPRESSIONNODE_H
#define EGCEXPRESSIONNODE_H

/**
 * @brief The EgcExpressionNode class defines the base class for all expressions (multiplication, substraction, ...) in the expression tree.
 * The class is built up as composite pattern.
 */
class EgcExpressionNode
{
public:
        EgcExpressionNode();
};

#endif // EGCEXPRESSIONNODE_H
